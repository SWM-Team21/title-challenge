spring:
  datasource:
    url: # jdbc:mysql://{AWS RDS ?????}:3306/{DB ??}?characterEncoding=UTF-8
    username: # DB ??? ?? USERNAME
    password: # DB ??? ?? ????
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace